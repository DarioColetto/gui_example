GUI application with porpuse of showing Tkinter(windows, widgets, some configurations, etc..)
Performs a basic Login and Registration.

Libraries/Modules used:

-Tkinter for GUI, 
-Shelve to perform persistance.
-sys & os to solve module import errors.
-Path library to make paths available through folders
